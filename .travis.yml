language: node_js
node_js:
  - "node"
cache:
  directories:
  - node_modules
before_script:
  - npm install -g firebase-tools
script:
- npm test
- npm run lint
- npm run build
after_success:
  - firebase deploy --token $FIREBASE_TOKEN
