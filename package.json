{
  "name": "violet",
  "version": "3.0.2",
  "description": "Customer Relationship Manager for C-Point LTD",
  "main": "server.js",
  "scripts": {
    "client": "npm start --prefix client",
    "client-build": "npm run build --prefix client",
    "client-deploy": "npm run client-test && npm run client-build && firebase deploy -P default",
    "client-deploy-dev": "npm run client-test && npm run client-build && firebase deploy -P development",
    "client-install": "npm install --prefix client",
    "client-test": "npm test --prefix client",
    "lint": "eslint client/src --fix",
    "watch-client": "npm run watch --prefix client"
  },
  "author": "Yaron Malin",
  "devDependencies": {
    "babel-eslint": "^8.2.3",
    "babel-jest": "^23.0.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "eslint": "^4.19.1",
    "eslint-config-google": "^0.9.1",
    "eslint-plugin-jest": "^21.15.1",
    "eslint-plugin-react": "^7.7.0",
    "firebase-tools": "^3.18.5"
  }
}
